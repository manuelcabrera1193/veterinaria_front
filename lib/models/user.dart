class User {
  bool isLogged;
  bool isAdmin;
  String name;
  String correo;

  User(
      {required this.isLogged,
      required this.isAdmin,
      required this.name,
      required this.correo});
}
